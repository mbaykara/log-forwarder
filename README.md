# Log Forwarder
The Log Forwarder behave like Fluent-bit http plugin so that Fluen-bit can send the collected logs to Log Forwarder.
It forwards your logs to azure blob storage either with defined Interval or file size. The logs comes from Fluent-bit instances.
