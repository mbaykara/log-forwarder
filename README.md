# Log Forwarder
The Log Forwarder is a Fluent-bit HTTP plugin so that Fluen-bit can send the collected logs to Log Forwarder.
It forwards your logs to Azure blob storage either with a defined Interval or file size. The logs come from Fluent-bit instances.
