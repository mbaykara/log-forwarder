# Raw Log Forwarder
is a service that i.e could be used with fluent-bit `http` plugin. It will recieve logs from Fluent-bit agent and send them to Azure Blob Storage.


## Requirement
`go version go1.18.2 linux/amd64`
